import { render } from '@testing-library/react';

// import { Posts } from 'modules/posts/components/Posts';

describe('Home', () => {
    it('renders', () => {
        // render(<Posts posts={[]} />);
        render(<div>TODO: tests</div>);
    });
});
